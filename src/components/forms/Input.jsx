const Input = (props) => (
  <input
    {...props}
    style={{
      width: '100%',
      borderRadius: 12,
      border: '1px solid #e9e9e9',
      padding: '0.75rem 0.9rem',
      fontSize: '0.95rem',
      background: '#fff'
    }}
  />
);

export default Input;
