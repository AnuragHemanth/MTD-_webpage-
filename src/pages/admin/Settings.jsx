import Card from '../../components/layout/Card';

const Settings = () => (
  <Card>
    <h2 style={{ marginTop: 0 }}>Admin settings</h2>
    <p style={{ margin: 0, color: '#555' }}>Platform settings and access policies will be configured here in a future update.</p>
  </Card>
);

export default Settings;
